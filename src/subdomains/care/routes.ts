import CareAddContainer from './containers/care-add-container';
import { CareEditWrapperContainer } from './containers/care-edit-wrapper.container';
import { CareAllContainer } from './containers/care-all.container';

export const routes = {
  ADD: CareAddContainer,
  EDIT: CareEditWrapperContainer,
  ALL: CareAllContainer,
};
